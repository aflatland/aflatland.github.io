{
  "homepage": "http://aflatland.github.io",
  "dependencies": {
    "heroicons": "^2.1.1"
  },

  "scripts": {
    "predeploy": "cd frontend && npm run build",
    "deploy": "gh-pages -d build"
  },

  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
